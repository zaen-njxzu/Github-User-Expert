<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zaen.githubuser">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission android:name="com.zaen.githubuser.provider.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.zaen.githubuser.provider.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:name=".GithubUsersApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name=".db.provider.UserContentProvider"
            android:authorities="com.zaen.githubuser.provider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.zaen.githubuser.provider.READ_DATABASE"
            android:writePermission="com.zaen.githubuser.provider.WRITE_DATABASE"/>

        <activity android:name=".ui.GithubUsersActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>